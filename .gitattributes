# Shell scripts must keep LF (run on Linux/macOS)
*.sh text eol=lf

# Windows scripts can use CRLF
*.ps1 text eol=crlf
*.bat text eol=crlf

# Go source
*.go text eol=lf

# Everything else — let Git decide per platform
* text=auto
